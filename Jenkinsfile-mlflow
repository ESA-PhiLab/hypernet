pipeline {
    agent { label "${params.label}" }
        stages{
            stage('prebuild') {
                steps {
                    sh "bash -c '${params.command_prebuild}'"
                }
            }
            stage('RUN') {
                steps {
                    sh "bash -c '${params.command_run}'"
                }
            }
        }
}

